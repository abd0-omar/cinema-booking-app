# Copy to hurl/vars.env (gitignored) for tasks.hurl and movies.hurl.
base_url=http://127.0.0.1:3000
token=test-bearer-token
# Movie POST/PUT/DELETE require admin (see AGENTS.md Hurl smoke tests).
admin_token=test-admin-bearer-token
# First demo movie from db/seeds.sql (after migrate + seed) — optional for booking hurl flows
demo_movie_slug=money-heist-1
