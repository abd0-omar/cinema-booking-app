[package]
name = "cinema-booking-store-port"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
async-trait = "0.1"
chrono = { version = "0.4", features = ["serde"] }
cinema-booking-db = { path = "../../db" }
serde = { version = "1.0", features = ["derive"] }
thiserror = "2.0"
